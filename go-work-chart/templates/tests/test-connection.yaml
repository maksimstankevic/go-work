apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-work-chart.fullname" . }}-test-connection"
  labels:
    {{- include "go-work-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-work-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
